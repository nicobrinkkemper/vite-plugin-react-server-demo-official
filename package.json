{
  "name": "vite-plugin-react-server-demo-official",
  "repository": "https://github.com/nicobrinkkemper/vite-plugin-react-server-demo-official",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "demo": "rm -f todos.db && NODE_ENV=production PUBLIC_ORIGIN='http://localhost:3000' BASE_URL='/' npm run build && NODE_ENV=production node dist/server/server/index-*.js",
    "test": "npm run test:e2e",
    "test:e2e": "NODE_ENV=production PUBLIC_ORIGIN='http://localhost:3000' BASE_URL='/' npm run build && playwright test",
    "dev:rsc": "npm run env:dev-server -- vite",
    "dev:ssr": "npm run env:start -- vite",
    "preview": "npm run build:static:preview && npm run build:client:preview && npm run build:server:preview && npm run preview:start",
    "env:gh": "GITHUB_PAGES=true PUBLIC_ORIGIN='https://nicobrinkkemper.github.io' BASE_URL='/vite-plugin-react-server-demo-official/'",
    "env:preview": "PUBLIC_ORIGIN='http://localhost:4173' BASE_URL='/vite-plugin-react-server-demo-official/'",
    "env:dev": "NODE_ENV=development BASE_URL='/' PUBLIC_ORIGIN='http://localhost:5173'",
    "env:dev-server": "NODE_OPTIONS='--conditions react-server' npm run env:dev",
    "env:start": "NODE_ENV=development BASE_URL='/' PUBLIC_ORIGIN='http://localhost:5173'",
    "build": "NODE_OPTIONS='--conditions react-server' vite build --app",
    "build:static": "vite build",
    "build:client": "vite build --ssr",
    "build:server": "NODE_OPTIONS='--conditions react-server' vite build",
    "build:gh": "npm run build:static:gh && npm run build:client:gh && npm run build:server:gh",
    "build:static:gh": "npm run env:gh -- vite build",
    "build:client:gh": "npm run env:gh -- vite build --ssr",
    "build:server:gh": "npm run env:gh -- NODE_OPTIONS='--conditions=react-server' vite build --ssr",
    "build:preview": "npm run build:static:preview && npm run build:client:preview && npm run build:server:preview",
    "build:static:preview": "npm run env:preview -- vite build",
    "build:client:preview": "npm run env:preview -- vite build --ssr",
    "build:server:preview": "npm run env:preview -- NODE_OPTIONS='--conditions=react-server' vite build --ssr",
    "preview:start": "npm run env:preview -- vite preview",
    "build:static:dev": "npm run env:dev -- vite build --mode development",
    "build:client:dev": "npm run env:dev -- vite build --ssr --mode development",
    "build:server:dev": "npm run env:dev -- NODE_OPTIONS='--conditions=react-server' vite build --ssr --mode development",
    "debug-build": "npm run build:static:dev && npm run build:client:dev && npm run build:server:dev",
    "clean-install": "npm install react@^19.2.7 react-dom@^19.2.7",
    "patch-oss": "patch"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/express": "^5.0.6",
    "@types/node": "^22.20.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.7.0",
    "acorn-loose": "^8.5.2",
    "happy-dom": "^20.10.6",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "tsx": "^4.22.4",
    "vite": "^6.4.3",
    "webpack-sources": "^3.5.0"
  },
  "dependencies": {
    "vite-plugin-react-server": "^2.11.0"
  }
}
